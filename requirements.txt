# Install manifest for the Vercel Python runtime (project root = repo root).
#
# fastapi is listed explicitly so Vercel's framework detection selects the FastAPI
# preset -- detection reads this file's literal contents and would not see fastapi
# through the local path installs below.
fastapi>=0.111.0

# Local path packages. Order matters: workflow-core is a dependency of
# workflowguard-api and is not published to PyPI.
#
# Local development still uses `pip install -e packages/workflow-core -e apps/api[dev]`
# (see README); this file exists for the deploy, not to replace that.
./packages/workflow-core
./apps/api
